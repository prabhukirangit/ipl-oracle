{
  "name": "ipl-oracle",
  "version": "0.1.0",
  "description": "IPL Oracle - MiroFish-style swarm intelligence engine for IPL cricket prediction",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && uv run python run.py",
    "dev:frontend": "cd frontend && npm run dev",
    "setup": "npm run setup:backend && npm run setup:frontend",
    "setup:backend": "cd backend && uv sync",
    "setup:frontend": "cd frontend && npm install",
    "setup:all": "npm install && npm run setup",
    "build": "cd frontend && npm run build"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
